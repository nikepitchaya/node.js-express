<!DOCTYPE html>
<html>

<head>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/4.7.0/css/font-awesome.min.css">
    <style>
        .image {
            height: 400px;
            width: 400px;
        }


        .content {
            display: flex;
            margin: 1.5rem;
            padding: 0.5rem;
        }

        .card {
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            box-shadow: 0 4px 8px 0 rgba(0, 0, 0, 0.2);
            transition: 0.3s;
            width: 40%;
        }

        .card:hover {
            box-shadow: 0 8px 16px 0 rgba(0, 0, 0, 0.2);
        }

        .container {
            padding: 2px 16px;
        }
    </style>
</head>

<body>
    <%-include('navbar')%>
        <!-- Content -->
        <div style="padding-left:16px">
            <h2>Responsive Topnav Example</h2>
            <p>Resize the browser window to see how it works.</p>
        </div>
        <div class="content">
            <% Object.values(products).forEach(function(item){%>
                <div class="card">
                    <img src="/images/products/<%=item.image%>" class="image">
                    <div class="container">
                        <p>ชื่อสินค้า :<b>
                                <%=item.name%>
                            </b></p>
                        <p>ราคา :<b>
                                <%=item.price%>
                            </b></p>
                        <p>
                            รายละเอียด :<b>
                                <%=item.description%>
                            </b>
                        </p>
                    </div>
                    <a href="/product/<%=item._id%>">ดูเพิ่มเติม</a>
                </div>
                <%})%>
        </div>
</body>

</html>